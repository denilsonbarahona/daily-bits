import styled from "styled-components";

export const LivesContainer = styled.div`
    font: var(--body1-bold); 
    color: var(--red);
    font-size:1.5rem;
    text-align:center;
`

export const LivesCounter = styled.p`
    margin:0;
    display:inline-block; 
    padding:0;
    color: var(--white);
    vertical-align:middle;
    margin-inline-start:.25rem;
`