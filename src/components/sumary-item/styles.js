import styled from "styled-components";

export const SumaryLi = styled.li`
    color: var(--white);
    list-style: none;
    padding: 1rem;
    border: 1px solid var(--white);
    border-radius: .5rem;
    margin-block-end: 1rem;
    display: flex;
    justify-content: space-between;
`

