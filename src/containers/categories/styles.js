import styled from "styled-components";

export const Container = styled.div`
    display: grid;
    grid-template-columns: repeat(2, 9rem);
    justify-items: center; 
    place-content: center;
    row-gap: 1.5rem;
    margin-block-start: 2.06rem;
` 